export const metadata = {
    title: 'Dashboard',
    description: 'Manage your Untitled Charts profile and uploaded levels.',
};

export default function DashboardLayout({ children }) {
    return <>{children}</>;
}
