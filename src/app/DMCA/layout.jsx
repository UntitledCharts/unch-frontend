export const metadata = {
    title: 'DMCA Policy',
    description: 'Untitled Charts DMCA Guidelines and Copyright Infringement Reporting Policy.',
};

export default function DMCALayout({ children }) {
    return <>{children}</>;
}
